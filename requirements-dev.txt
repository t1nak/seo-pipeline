# Development dependencies. Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
# Then run:
#     pytest

pytest>=8.0
pytest-cov>=4.1   # optional, for coverage reports
